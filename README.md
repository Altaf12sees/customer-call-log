# customer-call-log
Etisalat Task
----------- packages are used ----------
#virtual env
python3 -m pip install --user virtualenv
python3 -m venv env
source env/bin/activate

pip3 install django
pip3 install mysqlclient